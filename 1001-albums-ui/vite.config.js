import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { VitePWA } from 'vite-plugin-pwa'

// https://vite.dev/config/
export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['disc.svg'],
      manifest: {
        name: '1001 Albums Journey',
        short_name: '1001 Albums',
        description: 'Track your progress through the 1001 Albums You Must Hear Before You Die.',
        theme_color: '#0f0f11',
        background_color: '#0f0f11',
        icons: [
          {
            src: 'pwa-192x192.png',
            sizes: '192x192',
            type: 'image/png'
          },
          {
            src: 'pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png'
          }
        ]
      }
    })
  ],
})
